# Create a data frame
df <- data.frame(
  Name = c("a","b","c"),
  Value = c(1, 2, 3)
)

# Displaye the data frame
print(df)

#plot the data frame using default parameters
plot(df)

#plot(df$Name, df$Value)

